apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: budo-app-ingress
  namespace: budo
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx # Explicitly define your controller
  tls:
  - hosts:
    - app.letsbudo.com
    secretName: budo-app-tls # Cert-manager will create this secret
  rules:
  - host: app.letsbudo.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: budo-app # MUST match the metadata.name of your Service
            port:
              number: 80