apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: schoolrider-web-ingress
  namespace: schoolrider
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/rewrite-target: /app$1
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx # Explicitly define your controller
  tls:
  - hosts:
    - web.schoolrider.com
    secretName: schoolrider-web-tls # Cert-manager will create this secret
  rules:
  - host: web.schoolrider.com
    http:
      paths:
      - path: /(.*)
        pathType: Prefix
        backend:
          service:
            name: schoolrider-web # MUST match the metadata.name of your Service
            port:
              number: 80