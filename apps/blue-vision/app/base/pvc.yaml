apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: blue-vision-app-files
  namespace: blue-vision
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi  # Adjust size as needed
  # storageClassName: standard # Only add if you have a specific storage class