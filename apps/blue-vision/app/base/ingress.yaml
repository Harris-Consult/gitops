apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: blue-vision-app-ingress
  namespace: blue-vision
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx # Explicitly define your controller
  tls:
  - hosts:
    - app.bluevisionltd.com
    secretName: blue-vision-app-tls # Cert-manager will create this secret
  rules:
  - host: app.bluevisionltd.com
    http:
      paths:
      - path: /app
        pathType: Prefix
        backend:
          service:
            name: blue-vision-app # MUST match the metadata.name of your Service
            port:
              number: 80