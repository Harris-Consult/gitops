apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cradle-api-files
  namespace: cradle
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi  # Adjust size as needed
  # storageClassName: standard # Only add if you have a specific storage class