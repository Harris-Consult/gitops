apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tkproster-api
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
    argocd.argoproj.io/sync-options: CreateNamespace=true
spec:
  project: default
  source:
    repoURL: https://github.com/Harris-Consult/gitops.git
    path: apps/tkproster/api/overlays/production
    targetRevision: main
  destination:
    server: https://kubernetes.default.svc
    namespace: tkproster
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - PrunePropagationPolicy=background
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  revisionHistoryLimit: 10
